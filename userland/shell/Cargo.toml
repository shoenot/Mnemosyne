[package]
name = "shell"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "shell"
test = false
bench = false

[dependencies]
vespertine-rt = { path = "../../lib/vespertine-rt" }
vespertine-abi = { path = "../../lib/vespertine-abi" }
